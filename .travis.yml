language: php

php:
  - 5.4
  - 5.5
  - 5.6
  - nightly
  - hhvm-nightly
  
matrix:
  allow_failures:
    - php: nightly
    - php: hhvm-nightly

before_script: composer install -n --no-scripts --no-plugins

script: phpunit

after_success: bin/simpspector ./ -f detail
